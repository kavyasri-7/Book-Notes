<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Book</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <h1>📚 Book Notes</h1>
    <nav>
      <a href="/">Home</a>
      <a href="/books/new">➕ Add Book</a>
    </nav>
  </header>

  <main>
    <h2>Edit Book</h2>
    <form action="/books/<%= book._id %>?_method=PUT" method="POST">
      <input name="title" value="<%= book.title %>" placeholder="Title" required>
      <input name="author" value="<%= book.author %>" placeholder="Author" required>
      <input name="rating" type="number" min="1" max="10" value="<%= book.rating %>" placeholder="Rating">
      <textarea name="notes" placeholder="Notes"><%= book.notes %></textarea>
      <input name="dateRead" type="date" value="<%= book.dateRead.toISOString().slice(0,10) %>" required>
      <button type="submit">Update Book</button>
    </form>
  </main>

 
</body>
</html>
